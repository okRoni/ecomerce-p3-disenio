import { View, Text } from 'react-native';

export default function ReserveScreen() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="font-bold">Módulo de reserva</Text>
    </View>
  );
}